/*
 * SunEdison.com Leaderboard plugin for jQuery
 *
 */
(function($){
  $.fn.extend({
    leaderboard: function( url ) {
      var leaderboard_view = new window.LeaderboardView( url );
      leaderboard_view.setText(); 
    }
  });
})(jQuery);
